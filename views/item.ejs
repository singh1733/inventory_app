<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Document</title>
  </head>
  <body>
    <h1><%= category.name %></h1>
    <h1><%= item.name %></h1>
    <% if (item.image_src) {%>
    <img src="<%= item.image_src %>" />
    <%}%>
    <p><%= item.description %></p>
    <a href="/category/<%= category.id %>/item/<%= item.id %>/edit">Edit Item</a>
    <form
      action="/category/<%= category.id %>/item/<%= item.id %>/delete"
      method="POST"
      style="display: inline"
    >
      <button
        type="submit"
        onclick="return confirm('Are you sure you want to delete this item?');"
      >
        Delete
      </button>
    </form>
  </body>
</html>
